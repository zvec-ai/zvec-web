---
title: 快速上手
description: 安装 Zvec-Grep、索引一个工作区，并在几分钟内完成一次有效检索。
icon: Rocket
---


## 开始之前

- Node.js 22 或更新版本
- macOS、Linux 或 Windows
- 一个包含代码、Markdown、文本或结构化数据的本地目录

## 1. 安装

```bash
npm install -g @zvec/zvec-grep
zg version
```

## 2. 索引工作区

在需要检索的工作区中运行：

```bash
cd /path/to/your/workspace
zg index --embedding local/potion-code-16m-v2
zg status --check-ready
```

示例模型适合作为源码工作区的轻量起点。文档或多语言内容请查看 [Embedding 模型](../embedding-models/)。本地工作区文件和生成的向量均留在本机。

## 3. 检索

不清楚具体措辞或位置时，按语义搜索：

```bash
zg query --human "主题偏好在启动时从哪里恢复" --limit 5
```

结果应包含经过排序的片段、文件路径和源码位置。如果没有结果，使用 `zg status` 确认工作区根目录与索引状态。

## 后续步骤

- [检索指南](../search/)：选择混合、BM25、向量或穷举式 ripgrep，并调整结果。
- [CLI 参考](../cli/)：查找主要命令和内置帮助。
- [管理索引](../indexing/)：更新、重建、检查或删除索引。
- [接入 AI Agent](../agents/)：配置 Codex、Claude Code、Qwen Code、Cursor 或 OpenCode。
- [故障排查](../troubleshooting/)：定位安装、索引、检索或连接问题。
