---
title: 分组查询
extendedTitle: 分组查询（即将推出）
---


分组查询将类似 SQL `GROUP BY` 的语义带入向量搜索。通过此功能，你将能够按指定的标量字段（例如 `category`）对检索结果进行分组，并在每个组内执行向量相似度搜索。

<Callout className="text-base" type="info">
    分组查询功能将在即将发布的版本中提供。
</Callout>
